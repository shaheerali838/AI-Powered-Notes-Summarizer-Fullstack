# Backend API URL for client-side fetch requests
# For local development: http://localhost:5000
# For single-project Vercel monorepo deployment: leave empty (uses relative /api routes)
# For separate Vercel frontend project: https://your-backend-project.vercel.app
VITE_APP_API_URL=http://localhost:5000
